<h1 align="center">Nuevo Usuario</h1>

<%= render 'form', user: @user %>

<% if @user.has_role?(:teacher) %>
	<%= link_to 'Atras', teachers_users_path %>
<% elsif @user.has_role?(:student) %>
	<%= link_to 'Atras', students_users_path %>
<% end %>
